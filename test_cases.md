# Тест-кейсы (5)

## 1. Регистрация входа сотрудника
Шаги:
1. Открыть вкладку «Сотрудники».
2. Ввести номер пропуска `EMP-001`, имя `Иван`, фамилию `Иванов`.
3. Нажать «Вход».

Ожидаемый результат:
- Появляется сообщение «Вход сотрудника зарегистрирован.»
- В базе создаётся запись `visits` с `entered_at`, `exited_at = NULL`.

## 2. Регистрация выхода сотрудника
Шаги:
1. После тест-кейса 1 на вкладке «Сотрудники» оставить номер пропуска `EMP-001`.
2. Нажать «Выход».

Ожидаемый результат:
- Появляется сообщение «Выход сотрудника зарегистрирован.»
- В последней открытой записи `visits` заполняется `exited_at`.

## 3. Создание временного пропуска гостя
Шаги:
1. Открыть вкладку «Гости».
2. Ввести ID `GUEST-01`, имя `Petr`, фамилию `Petrov`, часы `1`.
3. Нажать «Вход».

Ожидаемый результат:
- Появляется сообщение «Вход гостя зарегистрирован (временный пропуск создан).»
- В базе создаётся запись `visits` с `person_type='guest'`, `guest_valid_hours=1`.

## 4. Просрочка временного пропуска гостя
Предусловие:
- Есть активный гостевой вход с `guest_valid_hours=1` и временем входа более 1 часа назад.

Шаги:
1. Открыть вкладку «Гости».
2. Нажать «Проверить просрочки».

Ожидаемый результат:
- Появляется предупреждение с текстом просьбы обратиться к старшему сотруднику охраны.

## 5. Парковочный талон сотрудника с использованием гостевого места
Предусловие:
- В «Настройки» установить: мест для сотрудников = 0, мест для гостей = 1.

Шаги:
1. Открыть вкладку «Парковка».
2. Указать тип `employee`, пропуск `EMP-002`, номер авто `A123BC`.
3. Нажать «Выдать талон».

Ожидаемый результат:
- Талон выдаётся, несмотря на 0 мест для сотрудников, потому что есть свободное гостевое место.

